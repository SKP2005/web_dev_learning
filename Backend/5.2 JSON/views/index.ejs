<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <%if(locals.recipeName){ %>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->
      <% for(var i=0;i<recipeJSON.length;i++){%>
        <%if(recipeJSON[i].name.toLowerCase()===recipeName+" taco"){%>
          <h3>Ingredients:</h3>
          <ul id="ingredientsList">
            <li><%=recipeJSON[i].ingredients.protein.name%>, <%=recipeJSON[i].ingredients.protein.preparation%></li>
            <li><%=recipeJSON[i].ingredients.salsa.name%></li>
            <%for(var j=0;j<recipeJSON[i].ingredients.toppings.length;j++){%>
              <li><%=recipeJSON[i].ingredients.toppings[j].quantity%> of <%=recipeJSON[i].ingredients.toppings[j].name%></li>
            <%}%>
          </ul>
        <%}%>
      <%}%>
  
    <%}else{%>
     <!-- If no recipe yet, show: -->
      <h2>Pick your favourite taco ingredient 👆</h2>
    <%}%>



  </div>
</body>

</html>